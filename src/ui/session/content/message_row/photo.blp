using Gtk 4.0;

template $PaplMessagePhoto : $PaplMessageBase {
  layout-manager: BinLayout {};

  $PaplMessageBubble message_bubble {
    styles ["media"]

    prefix: $PaplMessageMediaPicture picture {};
  }
}
