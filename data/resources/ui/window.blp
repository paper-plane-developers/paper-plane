using Gtk 4.0;
using Adw 1;

template $Window : Adw.ApplicationWindow {
  width-request: 360;
  height-request: 200;
  default-width: 900;
  default-height: 600;

  content: $SessionManager session_manager {};
}
